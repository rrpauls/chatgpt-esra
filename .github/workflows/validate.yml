name: Validate skills

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Validate licensing
        run: |
          grep -q "Apache License" LICENSE
          test -s NOTICE
          grep -q '"license": "Apache-2.0"' plugin.json
          grep -q '"license": "Apache-2.0"' .codex-plugin/plugin.json
          grep -q 'agent-plugins.org/schemas/1.0.0/plugin.schema.json' plugin.json
          python -m json.tool .agents/plugins/marketplace.json >/dev/null
          grep -q "Apache-2.0 License" README.md
          python -m json.tool esra-conformance.json >/dev/null
          grep -q '"protocol_version": "1.2"' esra-conformance.json
      - run: python scripts/validate_skills.py
      - run: python scripts/esra_runtime.py --data-dir /tmp/chatgpt-esra-ci validate skills
      - run: python -m py_compile scripts/*.py
      - run: python -m unittest discover -s tests -v
      - run: python scripts/build_installer.py /tmp/esra-installer.zip
